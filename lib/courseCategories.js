/**
 * Єдине джерело правди для допустимих значень Course.category — і форма
 * створення/налаштувань курсу в /admin (вибір), і картка курсу в хабі
 * (показ тегу) орієнтуються на ту саму назву. Поки що значення одне —
 * "Продажі"; додавати нові теми сюди, в цей масив, без міграції (поле
 * звичайний String, не enum).
 */
export const COURSE_CATEGORIES = ["Продажі"];
