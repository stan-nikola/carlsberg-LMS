# TODO — план на завтра и дальше

Срез на конец сессии миграции vanilla JS + Google Sheets → Next.js +
Prisma + Postgres. Ветки: `migrate-to-nextjs` (вся функциональность),
`setup-deploy` (только конфиг для деплоя, не смёржена).

## 🔴 Блокирует всё остальное — нужно от вас

- [ ] **Реальный `DATABASE_URL`** (Postgres). Без него ничего из
      написанного кода нельзя ни разу проверить на реальных данных.
      Вариант на вечер — `npx prisma dev` поднимает локальный Postgres
      прямо в терминале, без внешнего хостинга.
- [ ] **Файл с сотрудниками** (email, код должности, кто кому
      подчиняется) — вы упомянули, что он есть. Пришлите его (путь к
      файлу или содержимое) — см. отдельный раздел ниже, там же список
      того, чего в нём, скорее всего, не хватит.
- [ ] Аккаунт на **resend.com** → `RESEND_API_KEY` (для писем с PIN).
- [ ] `SESSION_SECRET` и `CRON_SECRET` — сгенерировать самому
      (`openssl rand -hex 32`), никакого внешнего сервиса не нужно.

## 🗄️ Импорт сотрудников/оргструктуры (новое, из этого сообщения)

Прежде чем писать импорт-скрипт, нужно увидеть сам файл — формат
(CSV/Excel/JSON), точные колонки. Но заранее видно нестыковку со
схемой, которую стоит решить сразу, как пришлёте файл:

- [ ] **В схеме `Employee.externalCode`** (код из Monolit Agent —
      им сотрудник логинится, `ml03005` и т.п.) — **обязательное и
      уникальное** поле. В вашем файле, по описанию, есть email и код
      **должности** (`RM`/`ASM`/`SV`/`SR`/`MR`), но не код Monolit
      Agent для входа. Нужно решить: этот код есть в файле под другим
      названием, или его придётся заводить/собирать отдельно (тогда
      сотрудники без кода не смогут войти, пока их код не добавят).
- [ ] Как в файле закодирована иерархия — колонка "email
      руководителя", "ID руководителя" или дерево по отступам/уровням?
      От этого зависит, как резолвить `Employee.managerId`.
- [ ] Совпадают ли коды должностей в файле 1-в-1 с `Position.code`
      (`RM`/`ASM`/`SV`/`SR`/`MR`), которые уже в схеме — Position ещё
      не заведены, их тоже нужно создать (5 строк, тривиально) раньше
      импорта сотрудников.
- [ ] Территории (`Territory`) в файле есть? Если да — тоже придётся
      завести до импорта; если нет — территория у всех останется
      пустой, назначение курсов по региону будет недоступно, пока не
      добавите.
- [ ] После того как формат станет ясен — написать `prisma/seed-employees.js`
      (или отдельный `import-employees.js`) по образцу
      `prisma/seed.js`: сначала Position/Territory, потом Employee
      в два прохода (сначала все строки без managerId, потом
      проставить managerId — иначе не получится сослаться на ещё не
      созданного руководителя, если порядок в файле не строго сверху
      вниз).
- [ ] Кто получает `role: "admin"`/`"hr_manager"` — в файле должностей
      обычно этого нет, это отдельное решение (вручную пометить
      несколько email после импорта).

## ✅ Готово в этой сессии (для памяти)

- Next.js App Router + Prisma 7 + Postgres, весь vanilla-JS в `/legacy`
- Роли/видимость: `hasFullAccess`, `getAllSubordinates`,
  `getVisibleEmployeeIds` ([lib/permissions.js](lib/permissions.js)) —
  **написаны, но нигде не применены как фильтр** (пункт 4 ШАГ 1
  задания так и остался не сделан — ни одного API route со списком
  сотрудников/прогресса ещё нет, кроме курсов)
- Назначение курсов: `assignCourseToRole`,
  `assignCourseToPositionsAndTerritories`
  ([lib/courseAssignment.js](lib/courseAssignment.js)), ежедневный
  cron просрочки, админ-эндпоинт массового назначения
- Вход: код+PIN на почту руководителя, cookie-сессия, Resend
- Хаб (4 таба) + регистрация + курс-плеер (MVP) + админка контента
  курсов (raw JSON)
- `.env.example`, `build` со `prisma migrate deploy`, `engines.node`

## 🟡 Отложенный функционал (осознанно, MVP-решение при планировании)

Из `course-assortment.html` **не** перенесено — нужно решить, когда и
нужно ли вообще, для новых/существующих курсов:

- [ ] Аккордеон-гейт ("розгорніть картку, щоб продовжити")
- [ ] Streak-тосты с конфетти за серию правильных ответов
- [ ] Карта разделов / desktop outline
- [ ] i18n (en/kk) — в легаси они всё равно были пустыми на практике
- [ ] Хранение "на каком именно из N экранов" курса остановился
      сотрудник — сейчас это только `localStorage` на устройстве,
      как в legacy; для админских отчётов о том, кто на каком экране
      застрял, нужно завести поле в БД

## 🟡 Другие явные незавершённости

- [ ] `EnrollmentAttempt.longestCorrectStreak` всегда пишется `0` —
      подсчёт серии в плеере не реализован (см. пункт выше)
- [ ] Админка курсов ([app/admin/courses/[courseId]](app/admin/courses/[courseId]))
      — нет способа **создать** новый курс/модуль через UI, только
      редактировать существующие уроки. Новый курс сейчас заводится
      только руками через Prisma Studio/seed-скрипт
- [ ] Нет reset/смены PIN сотруднику вручную (админом) — если сотрудник
      потерял доступ, а `loginPin` уже стоит, сейчас его никак не
      сбросить кроме прямого похода в БД
- [ ] `Employee.role` (admin/hr_manager) выставляется только руками в
      БД — нет UI/эндпоинта для смены роли

## ШАГ 4 — то, что осталось после подготовки конфига

- [ ] Локально: `npx prisma migrate dev --name init` с реальным
      `DATABASE_URL` → создаст `prisma/migrations/`, закоммитить
- [ ] `npx prisma db seed` — наполнит курс «Асортимент» реальным
      контентом
- [ ] Импорт сотрудников (см. раздел выше) — до этого в системе не
      будет ни одного `Employee`, некому будет ни назначать курсы,
      ни логиниться
- [ ] Завести переменные окружения в Vercel (список — в прошлом
      сообщении в чате)
- [ ] Решить, когда мержить `setup-deploy` → `migrate-to-nextjs` →
      `main`, и когда делать первый деплой — я сам ни то ни другое не
      делаю без вашей явной команды
